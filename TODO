- DONE syntax for Debug.log: `log("a", "b")`
- DONE smart motives for case-of
- DONE remove the need for ; in many places
- DONE add `let {x,y} = ...` syntax for pair destruction
- DONE add `open x` for destruction
- DONE improve JS compiler
- DONE check a whole file with `fmfm file.fm`
- DONE show only first type error + hole errors, to avoid overwhelming user
- DONE show location on parse and type errors
- remove need for name on the do-notation
- lambdas with annotations `(b:A) x`
- done create HS compiler
- let case-of create a let for its "as" value
- implicit arguments
- more string escape sequences
